doctype html

block vars
  - var intro = 0
  - var demo = 'true'

html(svef-site='svef' svef-intro=intro svef-demo=demo)
  include _head

  body
    main
      block header
        include _header

      .inner
        block content
      .block.svef
      .block.awards
      .block.conf

    block footer
      include _footer

    block javascript
      script(src='/scripts/main.js')
